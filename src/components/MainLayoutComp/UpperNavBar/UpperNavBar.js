import React from 'react';

import styles from './UpperNavBar.module.css';

const UpperNavBar = (props) => {

    return (<div className={styles.upperNavBarContainer}>
        <span>This is the upper nav bar</span>
    </div>)
}

export default UpperNavBar;